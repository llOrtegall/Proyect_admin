import { Route, Routes } from "react-router-dom";

function App(): JSX.Element {
  return (
    <>
      <Routes>
        <Route path="/" element={ } />
        <Route path="/" element={ } />
      </Routes>
    </>
  )
}

export default App
